# The contract this portal is generated against.
#
# The OPERATOR contract, not the traveller one. Different document, different
# base path, different auth, and a tenant boundary the traveller API does not
# have: `operator_id` is read from the session row and is never accepted as a
# parameter in any position.
#
# The intent is that both frontends pin one yuvoy-api commit, so they cannot be
# reasoning about different versions of the same backend. They agree today:
# `yuvoy-app` is on this same ref. The two documents are separate files, so a
# change to one says nothing about the other — this bump touches only the
# operator document, and yuvoy-app's bump to the same ref touched only the
# traveller one.
#
# `pnpm contract:check` fails the build if this copy stops matching the ref.
repo=yuvoy-in/yuvoy-api
path=contracts/operator-openapi.yaml
# 9 Sep 2026, eighth bump. Forward from 1cbffaa07230. 287 lines added, 23
# removed, and every removal is a description being rewritten in place — no
# field and no response was taken away. This is the commit range that carries
# migration 0058, which yuvoy-operator#31 was reassigned to Hima for and which
# had not landed when that issue was written.
#
# Consumed in this change:
#   - `experienceId` + `role` on `POST /media/upload-intents` (op#31 §1). The
#     listing is named when the upload STARTS rather than after approval, so a
#     moderator judging NOT_THIS_EXPERIENCE has something to judge against.
#     Optional on the wire and it will become required; sending it is what
#     unblocks that.
#   - `experienceId` (REQUIRED) + `role` on `POST /media/photo-intents`, and
#     `intentId` through `/photo-intents/complete` (op#35). Required from the
#     start because nothing called the endpoint, so there was no deployed
#     client to break.
#   - `situation` on `GET /media` (op#35) — one server-computed word for what
#     an operator is looking at. `state` and `listing.state` are both still
#     true and deriving the answer from two enumerations client-side gets it
#     wrong in ways nobody notices for a month.
#   - `listing.listingState` on `GET /media` — the LISTING's state, which is a
#     different question from the PUBLICATION's `state`.
#   - `hero_taken` on the operator `ErrorCode`, and 409 on both intent routes
#     for a full gallery — refused before the upload rather than after twenty
#     minutes on a 0.5 Mbps uplink.
#   - 503 `media_unavailable` on `POST /media/{id}/publish`, which a photograph
#     can never produce and a draft listing never reaches.
#
# NOT consumed here, and each needs its own change:
#   - nothing outstanding.
#
# `git ls-remote` was the check for this ref, not `gh api .../commits/master`,
# which served a five-minute-stale SHA twice on 8 Sep and cost a retracted
# issue.
ref=e94901c1a543b3d63f0113d116f9e8c9ae94a960
branch=master
pinned_on=2026-09-09
