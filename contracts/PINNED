# The contract this portal is generated against.
#
# The OPERATOR contract, not the traveller one. Different document, different
# base path, different auth, and a tenant boundary the traveller API does not
# have: `operator_id` is read from the session row and is never accepted as a
# parameter in any position.
#
# Pinned to the same yuvoy-api commit yuvoy-app is pinned to, so the two
# frontends cannot be reasoning about different versions of the same backend.
#
# `pnpm contract:check` fails the build if this copy stops matching the ref.
repo=yuvoy-in/yuvoy-api
path=contracts/operator-openapi.yaml
# 5 Sep 2026: `AccountStanding` on `GET /me` — state, bookable, blocking[] and
# credentials[] (yuvoy-api#63 O3, PR #83). Consumed in the same change: /account
# stops claiming a PROSPECT is live, and says what is outstanding and who moves
# next. Also lands `POST /auth/signup` (operator self-onboarding, D-029), which
# this portal does NOT yet use — raised as yuvoy-operator#16.
ref=2a8f2eebafe7a2f0954932c991a677a9a5cf1eeb
branch=master
pinned_on=2026-09-05
