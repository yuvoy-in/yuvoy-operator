# The contract this portal is generated against.
#
# The OPERATOR contract, not the traveller one. Different document, different
# base path, different auth, and a tenant boundary the traveller API does not
# have: `operator_id` is read from the session row and is never accepted as a
# parameter in any position.
#
# The intent is that both frontends pin one yuvoy-api commit, so they cannot be
# reasoning about different versions of the same backend. They diverge while one
# of them is mid-integration: this ref is ahead of yuvoy-app's while the reels
# upload catches up with yuvoy-api#66 §3. The two documents are separate files,
# so being ahead here says nothing about the traveller contract — and the
# traveller one moved on the same day with a defect in it (yuvoy-api#92), which
# is the reason yuvoy-app has not followed.
#
# `pnpm contract:check` fails the build if this copy stops matching the ref.
repo=yuvoy-in/yuvoy-api
path=contracts/operator-openapi.yaml
# 6 Sep 2026, third bump. Forward from 03542275, which was master head when the
# reels attach work was written and had already been overtaken twice.
#
# Consumed in this change (yuvoy-operator#23): the join flow —
# `GET /join/{token}`, `POST /join/{token}/code`, `POST /join/{token}/accept` —
# plus `ADMIN` on the role model and `joinUrl` on `POST`/`GET /team`. Without
# them an owner could not get anybody onto their team at all: the invite
# produced a code nothing delivers and a link the screen threw away.
#
# Landed and NOT consumed, each on its own issue:
#   - `/experiences` + `/experiences/{id}`  — yuvoy-operator#22, Activities half
#   - `/profile`, `/credentials`, `/logo`   — yuvoy-operator#7 (O6)
#
# Still wrong upstream: `Manifest.screening` is one flag for a whole boat
# rather than one per party (yuvoy-api#103). yuvoy-operator#17 is blocked on it.
ref=a12944e58e5ce775481b324397eeb01b66fad5a0
branch=master
pinned_on=2026-09-06
