# The contract this portal is generated against.
#
# The OPERATOR contract, not the traveller one. Different document, different
# base path, different auth, and a tenant boundary the traveller API does not
# have: `operator_id` is read from the session row and is never accepted as a
# parameter in any position.
#
# The intent is that both frontends pin one yuvoy-api commit, so they cannot be
# reasoning about different versions of the same backend. They agree today:
# `yuvoy-app` is on this same ref. The two documents are separate files, so a
# change to one says nothing about the other — this bump touches only the
# operator document, and yuvoy-app's bump to the same ref touched only the
# traveller one.
#
# `pnpm contract:check` fails the build if this copy stops matching the ref.
repo=yuvoy-in/yuvoy-api
path=contracts/operator-openapi.yaml
# 8 Sep 2026, seventh bump. Forward from ab93328169fc, which was pinned five
# minutes before yuvoy-api merged 394358a982ee — the commit this pin is really
# about. 510 lines added, 7 removed, and every removal is a field being widened
# in place rather than taken away.
#
# Consumed in this change:
#   - `OperatorExperience.status` gains `not_selling` (yuvoy-operator#28 §1).
#     The copy and the Business link shipped on 7 Sep through the string path;
#     this pin is what lets `orderListings` narrow on the value properly.
#   - `POST /requests/{id}/accept` gains 409 `operator_not_sellable` (#28 §2),
#     already handled by code and now declared.
#   - `publishBlockers` on the listing (#30 §3) — the mandatory fields still
#     empty, in the same spelling the revision body uses. This is what the row
#     should render instead of a generic "cannot publish".
#   - `activityType` + `GET /catalog/vocabulary`'s `activityTypes` (#30 §2).
#     Credential requirements resolve per activity type now, not per category,
#     so a lapsed instructor certificate stops the scuba listing while the
#     operator's other listings keep selling.
#   - `GET /slots` gains `onSale`, `notOnSaleReason`, `notOnSaleDetail` (#30 §4)
#     — `notOnSaleDetail` is rendered VERBATIM, never mapped from the code, and
#     an unrecognised reason still needs a fallback.
#   - `POST /experiences/{id}/withdraw` (#30 §6).
#   - The widened operator `ErrorCode`, including `payload_too_large` (#30 §7).
#
# **`pricingUnit` lost its default**, which is the one change to read carefully:
# omitting it is now recorded as UNSTATED rather than as per_person, and
# `publishBlockers` carries it until somebody says which. The picker shipped on
# 7 Sep already sends an explicit choice and refuses a price without one, so it
# needs no change — it was built for exactly this.
#
# A note on how this pin was nearly missed: `gh api .../commits/master` served
# a SHA five minutes stale, twice, and an issue was raised against yuvoy-api on
# that reading and retracted. `git ls-remote` answers correctly and instantly,
# and is the check to run before claiming a ref is missing.
ref=1cbffaa0723027d3f7dc72c49fd67aa00548de53
branch=master
pinned_on=2026-09-08
